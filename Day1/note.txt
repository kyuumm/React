react是一个构建web和原生交互界面的库

创建react开发环境工具
npx create-react-app react-basic
npx:nodejs工具命令
中间：核心包，用于创建react项目
react-basic:react项目的名称

跑项目：
  cd react-basic
  npm start

  npm build是打包（开发完毕后使用）

JSX=javascript+XML(HTML)
    function App() {
      return (
        <div className="App">
          this is App
        </div>
      );
    }

优势：HTML的声明式模板写法，JS的可编程能力
是js的语法扩展，需要通过解析工具解析后才能运行
是React中编写UI模板的方式
运行网站：Babel

JSX中使用JS表达式，使用{}
1用引号传递字符串
2使用JavaScript变量
3函数调用和方法调用
4使用js对象
if，switch，变量声明是语句不是表达式，不能出现在{}中

假设：

count = 发工资的数字
React = 出工资条的 HR 系统
你自己 count++ = 在 Excel 里偷偷改工资（HR 不知道）

结果？

真工资不会涨

系统不会更新

Excel 只是你自己那一份变了

要正确涨工资，你必须走流程👇

setCount(新的工资) = 正常走审批 → 系统记录 → 工资条更新

-------更新b站评论：
1,json文件模拟数据，使用json-server库

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "server": "json-server db.json --port 3004"
  },
  新增server自定义指令
  下载axios前端请求库

  useEffect请求数据


封装评论项Item组件
  抽象原则:App作为“智能组件”负责数据的获取，ltem作为“U组件”负责数据的渲染